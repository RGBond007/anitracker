# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Node
node_modules/
dist/

# Secrets & local state
.env
*.sql.gz
docker/data/
# Per-machine compose tweaks — e.g. running a locally built image instead of the
# published one. Compose reads this automatically; it must never be committed.
docker-compose.override.yml
# Running the browser-local demo on your own box. Convenience, not something the
docker-compose.demo.yml

# OS / editors
.DS_Store
.idea/
.vscode/
tsconfig.tsbuildinfo
